# core/ (retired)

Python toolkit under `core/` was removed in mission **MIGRATION-AND-RETIREMENT**.

Use the Node CLI instead:

```text
npx --yes pnpm@10.12.1 --filter @fix-my-print/cli exec node dist/bin.js help
```

Deletion evidence: `project_plans/execution/phase-14/DELETION_MANIFEST.json` and `tests/differential/`.
