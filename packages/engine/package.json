{
  "name": "@fix-my-print/engine",
  "version": "0.0.0",
  "private": true,
  "description": "Deterministic analysis pipeline state machine",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "jest --config jest.config.cjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fix-my-print/contracts": "workspace:*",
    "@fix-my-print/formats": "workspace:*",
    "@fix-my-print/geometry": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "30.0.0",
    "@types/node": "24.2.0",
    "jest": "30.0.5",
    "ts-jest": "29.4.1",
    "typescript": "5.9.2"
  }
}
