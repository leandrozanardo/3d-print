# Phase 04 — Browser web shell scaffolding

## Objective

Introduce `@fix-my-print/web` (Vite + React + TypeScript) with capability diagnostics, file drop → geometry worker stub, progress/cancel, viewer placeholder, and default feature flags.

## Authorization

User directive: implement Fix My Print Phases 04, 11, 12 scaffolding.

## Deliverables

- `apps/web` workspace package
- Worker protocol + stub inspect
- Unit tests for capabilities and flag defaults
